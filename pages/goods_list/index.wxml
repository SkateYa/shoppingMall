<view>
    <SearchInput></SearchInput>
    <Tabs tabs="{{tabs}}" bindtabsItemChange="tabsItemChange">
        <block wx:if="{{tabs[0].checked}}">0</block>
        <block wx:elif="{{tabs[1].checked}}">1</block>
        <block wx:else>2</block>
    </Tabs>
</view>